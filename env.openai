# Configurazione per produzione con OpenAI
# Per usare questa configurazione, copia il contenuto in .env

# Provider
LLM_PROVIDER=openai
EMBEDDING_PROVIDER=openai

# API Key
OPENAI_API_KEY=sk-your-key-here

# Modelli OpenAI (opzionali, hanno default)
OPENAI_LLM_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-large

# Dimensione embedding (3072 per text-embedding-3-large)
EMBEDDING_DIMENSION=3072

