# Configurazione per produzione con Google
# Per usare questa configurazione, copia il contenuto in .env

# Provider
LLM_PROVIDER=google
EMBEDDING_PROVIDER=google

# API Key
GOOGLE_API_KEY=your-google-key-here

# Modelli Google (opzionali, hanno default)
GOOGLE_LLM_MODEL=gemini-pro
GOOGLE_EMBEDDING_MODEL=models/embedding-001

# Dimensione embedding (768 per Google)
EMBEDDING_DIMENSION=768

